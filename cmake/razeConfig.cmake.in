@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

if(@RAZE_ENABLE_OPENMP@)
    find_dependency(OpenMP REQUIRED COMPONENTS CXX)
endif()

include("${CMAKE_CURRENT_LIST_DIR}/razeTargets.cmake")

check_required_components(raze)
